name: Generate Profile Assets

on:
  schedule:
    - cron: "0 0,6,12,18 * * *"
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: profile-assets
  cancel-in-progress: false

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v4

      - name: Fetch previous assets
        uses: actions/checkout@v4
        with:
          ref: output
          path: output-branch
        continue-on-error: true

      - name: Prepare workspace
        run: |
          mkdir -p dist/stats dist/badges
          if [ -d output-branch ]; then
            cp -rn output-branch/. dist/ 2>/dev/null || true
          fi
          mkdir -p dist/stats dist/badges

      - name: Generate GitHub Stats card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: username=Mayur-Purohit&show_icons=true&include_all_commits=true&hide_border=true&bg_color=0d1117&title_color=C084FC&icon_color=A855F7&text_color=c9d1d9&ring_color=7C3AED&rank_icon=github
          path: dist/stats/github-stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Top Languages card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: username=Mayur-Purohit&layout=compact&include_all_commits=true&langs_count=8&hide_border=true&bg_color=0d1117&title_color=C084FC&text_color=c9d1d9&icon_color=A855F7
          path: dist/stats/top-languages.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Streak and Activity graph
        run: |
          fetch_svg() {
            url="$1"
            out="$2"
            for attempt in 1 2 3 4 5; do
              if curl -fsSL --retry 3 --retry-delay 8 --max-time 120 \
                -A "Mayur-Purohit-profile-assets" -o "$out" "$url" && grep -qi "<svg" "$out"; then
                echo "OK: $out (attempt $attempt)"
                return 0
              fi
              echo "Retry $attempt for $out"
              sleep 12
            done
            return 1
          }

          STREAK_URL="https://streak-stats.demolab.com?user=Mayur-Purohit&hide_border=true&background=0D1117&ring=7C3AED&fire=C084FC&currStreakLabel=A855F7&sideLabels=C084FC&dates=555555&currStreakNum=c9d1d9&sideNums=c9d1d9&stroke=7C3AED"
          GRAPH_URL="https://github-readme-activity-graph.vercel.app/graph?username=Mayur-Purohit&bg_color=0d1117&color=C084FC&line=7C3AED&point=EDE9FE&area_color=A855F7&area=true&hide_border=true&custom_title=Mayur%27s%20Contribution%20Activity"

          fetch_svg "$STREAK_URL" dist/stats/github-streak.svg || test -f dist/stats/github-streak.svg
          fetch_svg "$GRAPH_URL" dist/contribution-graph.svg || test -f dist/contribution-graph.svg

      - name: Generate Contribution Snake
        uses: Platane/snk/svg-only@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          github_user_name: Mayur-Purohit
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Write timestamps and status badges
        run: |
          UPDATED=$(date -u +"%Y-%m-%d %H:%M UTC")
          echo "$UPDATED" > dist/last-updated.txt
          BADGE_TIME=$(echo "$UPDATED" | sed 's/ /%20/g;s/:/%3A/g')
          curl -fsSL -o dist/badges/last-updated.svg \
            "https://img.shields.io/badge/Last_Updated-${BADGE_TIME}-7C3AED?style=for-the-badge&labelColor=0d1117"
          curl -fsSL -o dist/badges/auto-sync.svg \
            "https://img.shields.io/badge/Profile_Data-Auto_Synced-C084FC?style=for-the-badge&labelColor=0d1117&logo=github"

      - name: Verify required SVGs
        run: |
          for f in dist/stats/github-stats.svg dist/stats/top-languages.svg \
            dist/stats/github-streak.svg dist/contribution-graph.svg \
            dist/github-contribution-grid-snake.svg dist/github-contribution-grid-snake-dark.svg \
            dist/badges/last-updated.svg dist/badges/auto-sync.svg; do
            if [ ! -f "$f" ]; then
              echo "Missing: $f"
              exit 1
            fi
            if [[ "$f" == *.svg ]] && ! grep -qi "<svg" "$f"; then
              echo "Invalid SVG: $f"
              exit 1
            fi
            echo "Verified: $f"
          done

      - name: Publish to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
          keep_history: true
          commit_message: "chore: refresh profile analytics [skip ci]"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
